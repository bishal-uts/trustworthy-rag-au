# Trustworthy RAG for AU Financial Regulation — v0.1
# Python 3.11

# Retrieval
rank-bm25==0.2.2
faiss-cpu>=1.9.0,<2.0
numpy>=1.26,<3.0

# Embeddings + NLI
sentence-transformers>=3.0,<5.0
transformers>=4.40,<5.0
torch>=2.5,<3.0

# PDF parsing
pdfplumber>=0.11,<0.12

# LLM client (Ollama)
httpx>=0.27,<0.28

# Schemas + config
pydantic>=2.7,<3.0
pydantic-settings>=2.3,<3.0
PyYAML>=6.0,<7.0
jinja2>=3.1,<4.0

# UI
gradio>=5.0,<6.0    # 4.x had Starlette incompat (unhashable dict in TemplateResponse)
audioop-lts>=0.2.1; python_version >= "3.13"   # gradio -> pydub needs audioop, removed from stdlib in 3.13

# Utilities
tqdm>=4.66
rich>=13.7
